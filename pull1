<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-1BmE4kWBq78iYhFldvKuhfTAU6auU8tT94WrHftjDbrCEXSU1oBoqyl2QvZ6jIW3" crossorigin="anonymous">
    <script src="https://cdn.jsdelivr.net/npm/@popperjs/core@2.10.2/dist/umd/popper.min.js" integrity="sha384-7+zCNj/IqJ95wo16oMtfsKbZ9ccEh31eOz1HGyDuCQ6wgnyJNSYdrPa03rtR1zdB" crossorigin="anonymous"></script>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/js/bootstrap.min.js" integrity="sha384-QJHtvGhmr9XOIpI6YVutG+2QOK9T+ZnN4kzFN1RtK3zEFEIsxhlmWl5/YESvpZ13" crossorigin="anonymous"></script>
    <title>bootstrap</title>
    <link rel="stylesheet" href="./CSS.css">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.1.0/css/all.min.css" />

</head>
<body>
    <div class="container">
        heloworld
        
    </div>
   <div class="app"></div>
   <div class="parent">
        <div class="child">
            <button class="btn">
                submit
            </button>
        </div>
        <div class="child">
            <button class="btn1">
                color
            </button>
        </div>
    </div>
<div>
    <ul class="list">
      
    </ul>

</div>
        <i class="fa-brands fa-facebook">a</i>
        <i class="fa-brands fa-facebook-f"></i>
        <i class="fa-brands fa-facebook-f"></i>
        <i class="fa-solid fa-user"></i>
   
    <script>
        const app = document.querySelector('.btn');
        const list = document.querySelector('.list');
        app.onclick = ()=>{
            console.log("1");
        }
        window.onclick = function(e){
            const parent  = e.target.closest(".parent");
            console.log(parent);
            //khi kich vao vị trí của class parent or con của nó thì trả về nghuyên đọn class pảent
            if(app.classList.contains("app")){console.log(true)}
            //kha giong cai <i class="fa fa-transgender" aria-hidden="true"></i>
            
        }
         const arr= [1,2,3,4];
         const newArr = arr.map(a=>{
             return a*3
         })
         const li = arr.map(a=>{
             return '<li>'+a+'</li>\n';
              
         })
         const htmls= li.join("");
         // chuyển mảng thành chuỗi sát lại với nhau 
         list.innerHTML = htmls;
         console.log(htmls);
         console.log(li);
         console.log(newArr);
         const a = arr.find(a=>{
             return a == 4;
         })
         // tìm kiếm nếu tìm thấy trả về giá trị đó
         console.log(a);
         e=arr.slice(1,3);
         console.log(e);
         // cắt giá trị từ vị trí số 1 đén vị trí sô 3 nên dc 2 và 3
         gop = arr.indexOf(1);
         // trả về vị trí nếu ko có trả về -1

         console.log(gop);
         const item = {item: "dark"};
         const btn1 = document.querySelector('.btn1');
         const app1= document.querySelector('.app');
         const itemList= JSON.parse(localStorage.getItem('item')) || item;
         if(itemList.item == "dark"){
             btn1.classList.add("dark")

         }
         if(itemList.item != "dark"){
             btn1.classList.remove("dark")
             
         }
         btn1.onclick = () => {
             btn1.classList.toggle("dark");
             // kiểm tra nên trong class btn1 có chữ dark hay không có thì xóa ko có thì thêm vào
            if(btn1.classList.contains("dark")){
                itemList.item = "dark";

            }
            if(!btn1.classList.contains("dark")){
                itemList.item = "white";

            }
            console.log(itemList);
            localStorage.setItem('item', JSON.stringify(itemList));
        }
 </script>
</body>
</html>